import { StyleSheet } from '@breakaway/react-styles';
import { global_spacer_3xl as spacer3xl } from '@breakaway/react-tokens';

export default StyleSheet.create({
  content: {
    padding: spacer3xl.var
  }
});
